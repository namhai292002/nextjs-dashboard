import { Charis_SIL, Roboto_Mono, Lusitana } from 'next/font/google';

export const inter = Charis_SIL({
  subsets: ['latin'],
  weight: '400',
});

export const roboto_mono = Roboto_Mono({
  subsets: ['latin'],
  display: 'swap',
});

export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: '400',
});
